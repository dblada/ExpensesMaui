<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiAppExp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiAppExp"
    xmlns:v="clr-namespace:MauiAppExp.Views"
    Title="MauiAppExp">
    
    <!-- Zakładki dolne -->
    <TabBar>
        <ShellContent Title="Start"
        ContentTemplate="{DataTemplate local:MainPage}" 
        Route="start" />
        
        <ShellContent
        Title="Transakcje"
        ContentTemplate="{DataTemplate v:TransactionPage}"
        Route="transakcje" />
    </TabBar>

    <!-- Menu boczne -->
    <!--<FlyoutItem Title="Ustawienia" Icon="settings.png">
        <ShellContent
            Title="Ustawienia"
            Route="settings"
            ContentTemplate="{DataTemplate views:SettingsPage}" />
    </FlyoutItem>-->
</Shell>
